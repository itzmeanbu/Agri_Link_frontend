# AgriLink frontend

Responsive agricultural market intelligence and marketplace landing experience. The brand name is configured in `index.html` and the API base URL is defined in `script.js` (`API_URL`).

Open `index.html` directly for a zero-install preview, or serve this folder with any static web server. The existing Express backend can be connected by replacing the small interaction handlers with its authentication and resource endpoints.
